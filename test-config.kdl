// Minimal smoke-test config: keybind that exercises MessagePlugin.
keybinds {
    shared {
        bind "Alt t" {
            MessagePlugin "file:TARGET_WASM" {
                payload "terminal"
            }
        }
    }
}
